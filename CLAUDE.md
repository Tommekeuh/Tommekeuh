# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a **GitHub profile repository** (`Tommekeuh/Tommekeuh`). Its sole file, `README.md`, is automatically displayed on the GitHub profile page for the user `Tommekeuh`. There is no application code, build system, test suite, or CI pipeline.

## What's Here

- `README.md` — the GitHub profile page content. Edits here are immediately visible on the public GitHub profile once pushed to `main`.

## Conventions

- The README uses standard GitHub Flavored Markdown.
- Emoji are already used in the existing content; keep any additions consistent with the existing informal, personal tone.
- The profile covers interests in **internet security** and **crypto/trading** — keep edits relevant to those themes unless the user explicitly requests otherwise.

## Making Changes

To update the profile, edit `README.md` directly and push to `main`. There are no build steps, linting checks, or tests to run.
